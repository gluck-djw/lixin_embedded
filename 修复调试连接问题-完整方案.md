# STM32F411CE 调试连接问题 - 完整解决方案

## 🔍 问题诊断结果

### 硬件状态
- ✅ J-Link 硬件正常（V9.40, 固件 2021-05-07）
- ✅ USB 连接正常
- ✅ 目标板供电正常（3.291V）
- ✅ SWD 物理连接正常（DAP 初始化成功）
- ❌ CPU 拒绝调试连接："Can not attach to CPU"

### 根本原因

**地址配置混乱导致的系统性问题：**

1. **应用程序配置错误**：
   - Linker Script: `0x08010000` (64KB offset)
   - 代码中 VTOR: `0x08010000`
   - 执行 `HAL_DeInit()` 导致外设复位（可能影响调试）

2. **Bootloader 配置**：
   - 期望应用在: `0x08003000` (12KB offset)
   - 接收固件后不跳转，直接死循环

3. **内存布局冲突**：
   ```
   0x08000000 ┬─────────────────┐
              │   Bootloader    │ 12KB (0x3000)
   0x08003000 ├─────────────────┤ ← Bootloader 期望应用起始地址
              │                 │
              │                 │
   0x08010000 │ ← 应用实际配置  │ 232KB
              │   的起始地址    │
              │   (错误！)      │
              │                 │
   0x0803D000 ├─────────────────┤
              │  OTA 备份区     │ 268KB
   0x08080000 └─────────────────┘
   ```

4. **"调试几次后连不上"的流程**：
   - 通过调试器直接烧录 → 应用运行在 0x08010000
   - 应用执行 `HAL_DeInit()` → 可能影响调试接口
   - 重启或地址冲突 → CPU 跑飞/无响应
   - J-Link 无法重连

---

## 🛠️ 立即恢复调试连接（3个方法）

### 方法 1: 复位时快速连接（软件方法）

1. **准备 J-Link Commander**：
   ```bash
   JLink.exe  # 或双击桌面图标
   ```

2. **执行连接命令**：
   ```
   connect
   Device: STM32F411CE
   TIF: SWD
   Speed: 4000
   ```

3. **当提示 "Connecting..." 时**：
   - 按目标板的复位按钮
   - 或快速断电再上电
   - **在上电后 100ms 内疯狂按回车键**

4. **连接成功后立即擦除**：
   ```
   halt
   erase
   ```

### 方法 2: BOOT0 引脚法（硬件方法，最可靠）

1. **断开目标板电源**
2. **BOOT0 引脚接 3.3V**（或拨到高电平位置）
3. **上电** → 芯片进入系统 Bootloader 模式
4. **连接 J-Link**：
   ```
   connect
   ```
5. **擦除 Flash**：
   ```
   erase
   ```
6. **BOOT0 恢复接 GND**

### 方法 3: 在调试器下保持复位（推断方法）

1. **在 J-Link Commander 中**：
   ```
   connect
   ```

2. **手动保持目标板的 NRST 引脚为低电平**

3. **在 J-Link 中执行**：
   ```
   halt
   erase
   ```

4. **释放 NRST**

---

## 📝 修复代码（已完成）

### 文件 1: 应用程序 Linker Script
**路径**: `17_app/MDK-ARM/17_app/17_app.sct`

```diff
- LR_IROM1 0x08010000 0x00070000  {    ; 错误：64KB offset
-   ER_IROM1 0x08010000 0x00070000  {
+ LR_IROM1 0x08003000 0x0003A000  {    ; 正确：12KB offset, 232KB size
+   ER_IROM1 0x08003000 0x0003A000  {
```

### 文件 2: 应用程序主函数
**路径**: `17_app/Core/Src/main.c`

```diff
- SCB->VTOR = 0x8000000 | 0x10000;  // 0x08010000 错误！
+ SCB->VTOR = 0x08003000;             // 与 Bootloader 一致
  __enable_irq();
- HAL_DeInit();  // 删除！会复位外设，破坏调试连接
```

### 文件 3: Bootloader 主函数
**路径**: `18_ota/03firmware/bootloader/.../main.c`

**添加了**：
- `JumpToApplication()` 函数：安全跳转到应用
- 接收完固件后自动跳转
- 栈地址有效性检查
- 详细日志输出

---

## 🔄 完整操作流程

### 步骤 1: 恢复调试连接
按照上面"立即恢复调试连接"的任意方法执行。

### 步骤 2: 重新编译应用程序
```bash
cd D:/ecnu/work/work_space/lixin_embedded/17_app/MDK-ARM
# 在 Keil 中打开项目
# Project → Rebuild All
```

**检查编译输出**：
```
Program Size: Code=xxxx RO-data=xxxx RW-data=xxxx ZI-data=xxxx  
FromELF: creating hex file...
 - 0x08003000 开头  ← 确认地址正确
```

### 步骤 3: 烧录应用程序
```bash
# 方法 A: 通过 J-Link Commander
loadfile 17_app.bin 0x08003000

# 方法 B: 通过 Keil 下载按钮
```

### 步骤 4: 重新编译 Bootloader
```bash
cd D:/ecnu/work/work_space/lixin_embedded/18_ota/03firmware/bootloader/std_library_project
# 在 Keil 中编译
```

### 步骤 5: 测试完整流程

1. **烧录 Bootloader**：
   ```
   loadfile bootloader.bin 0x08000000
   ```

2. **复位运行**：
   ```
   reset
   go
   ```

3. **查看串口日志**：
   ```
   Bootloader started
   Attempting to jump to application...
   Jumping to app at 0x08003000
   ```

4. **应用程序开始运行**：
   ```
   hello world!
   ```

---

## ⚠️ 重要注意事项

### 关于 YMODEM 固件更新流程

**当前问题**: YMODEM 接收固件到 `0x0803D000`（BackAppAddress），但应用需要在 `0x08003000` 运行。

**需要额外实现**（Bootloader 中）：
```c
// 在 Ymodem_Receive 返回后添加
if (size > 0)
{
    // 1. 擦除应用区域 (0x08003000 - 0x0803D000)
    // 2. 从 BackAppAddress 复制到 ApplicationAddress
    // 3. 校验 CRC/Checksum
    // 4. 跳转到应用
}
```

**或者修改 YMODEM 接收目标**：
```c
// 在 ymodem.c 中修改
FlashDestination = ApplicationAddress;  // 改为 0x08003000
```

### 保护调试接口

**永远不要在应用程序中**：
```c
// ❌ 禁止
HAL_DeInit();                           // 会复位所有外设
GPIO_PinRemapConfig(..., DISABLE);      // 禁用 SWD/JTAG
__HAL_RCC_GPIOA_CLK_DISABLE();          // 关闭 PA13/PA14 时钟
HAL_PWR_EnterSTOPMode(...);             // 无条件进入低功耗

// ✅ 安全做法
#ifdef DEBUG
    // 调试模式下保持 SWD 使能
    __HAL_DBGMCU_FREEZE_IWDG();
    __HAL_DBGMCU_UNFREEZE_TIM2();
#endif
```

### 内存布局推荐

**STM32F411CE (512KB Flash)**：
```
0x08000000 - 0x08003000  (12KB)   Bootloader
0x08003000 - 0x08040000  (244KB)  Application
0x08040000 - 0x08080000  (256KB)  OTA 备份区

或者更安全的分配：
0x08000000 - 0x08008000  (32KB)   Bootloader
0x08008000 - 0x08044000  (240KB)  Application  
0x08044000 - 0x08080000  (240KB)  OTA 备份区
```

---

## 🎯 验证清单

完成后验证以下项目：

- [ ] J-Link 能稳定连接到目标板
- [ ] Bootloader 启动后有日志输出
- [ ] Bootloader 能跳转到应用
- [ ] 应用程序正常运行（串口输出 "hello world!"）
- [ ] 断电重启后系统正常
- [ ] 在应用运行期间能进入调试模式（设置断点）
- [ ] YMODEM 固件接收功能正常

---

## 📞 如果问题仍然存在

1. **检查硬件连接**：
   - SWDIO (PA13) → J-Link Pin 7
   - SWCLK (PA14) → J-Link Pin 9
   - GND → J-Link Pin 4
   - VCC → J-Link Pin 1 (可选，仅供电检测)

2. **检查供电**：
   - 3.3V 稳定供电
   - 去耦电容完好

3. **检查晶振**：
   - HSE 是否焊接/启动
   - 如果没有外部晶振，确保代码使用 HSI

4. **查看完整日志**：
   ```bash
   JLink.exe -log jlink.log
   ```

---

生成时间: 2026-07-29
